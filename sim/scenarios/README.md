# Scenario fixtures

Drop scenario text files here. Each file should be parseable by
`ScenarioParser.parse_file`.

## Example naming
- `earth_alpha_basic_distance.txt`
- `relay_delay_old_checkpoint.txt`
- `double_lock_conflict.txt`

Keep each file UTF-8 and keep line-based format simple for early parser extension.
